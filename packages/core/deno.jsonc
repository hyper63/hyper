{
  "tasks": {
    "test": "deno lint && deno fmt --check && deno test -A",
    "cache": "deno cache --lock=deno.lock --lock-write deps.ts dev_deps.ts mod.test.ts"
  },
  "fmt": {
    "include": ["./"],
    "lineWidth": 100,
    "singleQuote": true,
    "semiColons": false
  }
}
